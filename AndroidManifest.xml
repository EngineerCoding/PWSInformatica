<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ameling.grademanager"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="12"
              android:targetSdkVersion="21"/>

    <application
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo"
            android:icon="@drawable/ic_launcher"
            >
        <activity android:name=".MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".grade.SetupActivity"
                  android:label="New Subject"/>
        <!-- TODO @strings -->

        <activity android:name=".school.IntegratedSchoolActivity"
                  android:label="Choose formula"> <!-- TODO @strings -->
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data
                    android:name="android.app.searchable"
                    android:resource="@xml/school_searchable"/>
        </activity>

        <activity android:name=".school.SubjectDialogActivity"
                  android:label="Choose subject"
                  android:screenOrientation="portrait"
                  android:theme="@android:style/Theme.Holo.Dialog.NoActionBar"/>
        <activity android:name=".grade.ShowActivity"
                  android:label="Grade Tree"
                  android:windowSoftInputMode="adjustPan"/>
    </application>
</manifest>
